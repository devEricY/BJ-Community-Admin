<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "mybatis-3-mapper.dtd">

<mapper namespace="com.bjcommunity.admin.Mapper">
    <select id="" resultType="int" parameterType="AdminDTO">
        SELECT
        COUNT(ADMIN_ID)
        FROM TB_ADMIN
        WHERE
        ADMIN_ID = #{admin_id}
    </select>
</mapper>